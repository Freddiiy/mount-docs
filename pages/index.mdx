import Bleed from 'nextra-theme-docs/bleed'

# Mountie

**Mountie**'s backend is an api that can automatically authenticate and fetch information of characters and the
mounts they own. It can also inspect specific mounts and get detailed information about it.

![Mountie Logo](../public/og.png)
